---
phase: 04-recipe-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/features/recipes/presentation/recipe_browse_screen.dart
  - lib/features/recipes/presentation/recipe_card.dart
  - lib/features/recipes/presentation/providers/recipe_search_provider.dart
  - lib/features/recipes/presentation/widgets/filter_chips_row.dart
  - lib/router/app_router.dart
autonomous: true
requirements:
  - RECP-01
  - RECP-04

must_haves:
  truths:
    - "User can type a search query and see recipe results from Spoonacular"
    - "User can filter recipes by cuisine type and maximum cook time"
    - "User can tap 'Find recipes using my ingredients' and see only matching recipes"
    - "User can scroll through paginated results that load more on demand"
  artifacts:
    - path: "lib/features/recipes/presentation/recipe_browse_screen.dart"
      provides: "Recipe browse screen with search bar, filter chips, and paginated recipe list"
      min_lines: 80
    - path: "lib/features/recipes/presentation/recipe_card.dart"
      provides: "Recipe card widget with thumbnail, title, cook time"
      min_lines: 30
    - path: "lib/features/recipes/presentation/providers/recipe_search_provider.dart"
      provides: "Riverpod provider for paginated recipe search"
      contains: "@riverpod"
    - path: "lib/features/recipes/presentation/widgets/filter_chips_row.dart"
      provides: "Horizontal row of cuisine and cook time filter chips"
  key_links:
    - from: "lib/features/recipes/presentation/providers/recipe_search_provider.dart"
      to: "lib/features/recipes/data/recipe_repository.dart"
      via: "ref.watch(recipeRepositoryProvider)"
      pattern: "recipeRepositoryProvider"
    - from: "lib/features/recipes/presentation/recipe_browse_screen.dart"
      to: "lib/features/recipes/presentation/providers/recipe_search_provider.dart"
      via: "ref.watch for search results"
      pattern: "recipeSearchPageProvider"
    - from: "lib/features/recipes/presentation/recipe_card.dart"
      to: "recipe detail screen route"
      via: "onTap navigates to detail with recipe id"
      pattern: "context.push.*recipe"
---

<objective>
Build the recipe browse screen with search, filter chips, paginated results, and "find by my ingredients" functionality.

Purpose: This is the primary recipe discovery surface (RECP-01, RECP-04). Users need to find recipes by name/cuisine/cook time or by the ingredients they already have selected.
Output: Recipe browse screen, recipe card widget, filter chips, Riverpod search provider, route registration.
</objective>

<execution_context>
@C:/Users/tristan.keick/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tristan.keick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-recipe-discovery/04-RESEARCH.md
@.planning/phases/04-recipe-discovery/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Riverpod search provider with pagination and filter state</name>
  <files>
    lib/features/recipes/presentation/providers/recipe_search_provider.dart
  </files>
  <action>
    Create `lib/features/recipes/presentation/providers/recipe_search_provider.dart`:

    1. Define a @riverpod FutureProvider.family-style provider `recipeSearchPage` that accepts parameters: query (String), cuisine (String?), maxReadyTime (int?), includeIngredients (String?), page (int). Page size constant = 20.
       - Calls `ref.watch(recipeRepositoryProvider).searchRecipes(...)` with offset = page * pageSize.
       - Returns `RecipeSearchResult`.

    2. Define a @riverpod FutureProvider `ingredientBasedRecipes` that accepts ingredientNames (List<String>).
       - Calls `ref.watch(recipeRepositoryProvider).findByIngredients(ingredientNames)`.
       - Returns `List<RecipeSummary>`.

    3. Define a @riverpod Notifier `recipeFilterState` that holds the current filter selections:
       - query (String, default '')
       - cuisine (String?, default null)
       - maxReadyTime (int?, default null)
       - isIngredientMode (bool, default false) — when true, show ingredient-based results instead of search results
       - Methods: setQuery, setCuisine, setMaxReadyTime, toggleIngredientMode, clearFilters

    Use Riverpod 3.x codegen (@riverpod annotation). Do NOT use StateNotifierProvider (legacy).

    IMPORTANT: The page-keyed provider enables Riverpod to cache each page independently. `ref.invalidate` on filter change clears all cached pages.
  </action>
  <verify>
    <automated>cd C:/Users/tristan.keick/source/repos/MealMate && dart run build_runner build --delete-conflicting-outputs && dart analyze lib/features/recipes/presentation/providers/</automated>
  </verify>
  <done>recipeSearchPageProvider, ingredientBasedRecipesProvider, and recipeFilterStateProvider exist, use @riverpod codegen, and compile without errors. No import of riverpod/legacy.dart.</done>
</task>

<task type="auto">
  <name>Task 2: Recipe browse screen with search bar, filter chips, paginated list, and ingredient mode</name>
  <files>
    lib/features/recipes/presentation/recipe_browse_screen.dart,
    lib/features/recipes/presentation/recipe_card.dart,
    lib/features/recipes/presentation/widgets/filter_chips_row.dart,
    lib/router/app_router.dart
  </files>
  <action>
    1. Create `lib/features/recipes/presentation/widgets/filter_chips_row.dart`:
       - Horizontal scrollable row of FilterChip widgets.
       - Cuisine chips: Italian, Mexican, Chinese, Indian, Japanese, Mediterranean, American, Thai (hard-coded list from Spoonacular docs).
       - Cook time chips: Under 15 min, Under 30 min, Under 60 min (mapped to maxReadyTime values 15, 30, 60).
       - A special "Use my ingredients" ChoiceChip that toggles ingredient mode.
       - Selected state comes from `ref.watch(recipeFilterStateProvider)`.
       - On tap, calls the corresponding setter on the notifier.

    2. Create `lib/features/recipes/presentation/recipe_card.dart`:
       - Takes a RecipeSummary (id, title, image).
       - Displays CachedNetworkImage for thumbnail (120x90, BoxFit.cover), title text, and optionally cook time if available.
       - Placeholder: grey box with CircularProgressIndicator. Error: Icons.restaurant.
       - onTap: navigates to recipe detail route with recipe id.

    3. Create `lib/features/recipes/presentation/recipe_browse_screen.dart`:
       - AppBar with search TextField. On submit or debounced change (300ms), updates recipeFilterStateProvider.setQuery.
       - Below AppBar: FilterChipsRow widget.
       - Body: Conditionally renders based on `isIngredientMode`:
         - **Search mode (default):** ListView.builder with infinite scroll pagination. For each index, compute `page = index ~/ 20` and `indexInPage = index % 20`. Watch `recipeSearchPageProvider(query: ..., page: page)`. Show loading indicator for pending pages. Show "No results" empty state when totalResults == 0.
         - **Ingredient mode:** Watch `ingredientBasedRecipesProvider(ingredientNames)` where ingredientNames comes from the ingredient selection state (Phase 3's "I have these" state). Show list of RecipeCard widgets. Show "Select ingredients first" empty state if no ingredients selected.
       - Handle error states: Show "Daily recipe limit reached" on QuotaExhaustedException. Show generic retry-able error for other failures.

    4. Register the recipe browse route in `lib/router/app_router.dart`. Add `/recipes` route pointing to RecipeBrowseScreen. Ensure recipe card onTap pushes to `/recipes/:id` (detail route will be wired in Plan 03).

    IMPORTANT: Use CachedNetworkImage (not Image.network) for all recipe thumbnails. Handle null/empty image URLs gracefully with the error widget fallback.
  </action>
  <verify>
    <automated>cd C:/Users/tristan.keick/source/repos/MealMate && dart analyze lib/features/recipes/presentation/ lib/router/</automated>
  </verify>
  <done>Recipe browse screen renders with search bar, 3 filter dimensions (cuisine, cook time, ingredient mode), paginated recipe card list in search mode, ingredient-based list in ingredient mode, empty states for no results and no ingredients, and error handling for quota exhaustion. Route registered at /recipes.</done>
</task>

</tasks>

<verification>
- `dart analyze lib/features/recipes/presentation/` passes with no errors
- Recipe browse screen has search, filter chips, and two display modes (search vs ingredient)
- No usage of `StateNotifierProvider` or `riverpod/legacy.dart` imports
- All recipe thumbnails use CachedNetworkImage
- Route `/recipes` is registered in app_router.dart
</verification>

<success_criteria>
- User can type a query and see paginated recipe search results (RECP-01: search by name)
- User can select cuisine and cook time filters and results update (RECP-01: search by cuisine, cook time)
- User can toggle "Use my ingredients" mode and see filtered results (RECP-04)
- Recipe cards show thumbnails, titles, and navigate to detail on tap
- Empty states display for no results and no ingredients selected
- Quota exhaustion (402) shows a user-friendly message
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-discovery/04-02-SUMMARY.md`
</output>
