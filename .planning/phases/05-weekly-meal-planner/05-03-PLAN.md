---
phase: 05-weekly-meal-planner
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - meal_mate/lib/features/meal_planner/presentation/widgets/planner_grid.dart
  - meal_mate/lib/features/meal_planner/presentation/widgets/meal_slot_card.dart
  - meal_mate/lib/features/meal_planner/presentation/widgets/empty_slot_card.dart
autonomous: true
requirements:
  - PLAN-04

must_haves:
  truths:
    - "User can long-press a filled meal card and drag it to another slot"
    - "Dragging onto a filled slot swaps the two meals"
    - "Dragging onto an empty slot moves the meal there"
    - "Swap and move persist on reload"
    - "Horizontal scroll does not conflict with drag gesture"
  artifacts:
    - path: "meal_mate/lib/features/meal_planner/presentation/widgets/planner_grid.dart"
      provides: "DragTarget wrappers on each slot cell"
      contains: "DragTarget"
    - path: "meal_mate/lib/features/meal_planner/presentation/widgets/meal_slot_card.dart"
      provides: "LongPressDraggable wrapper on filled slots"
      contains: "LongPressDraggable"
  key_links:
    - from: "planner_grid.dart"
      to: "meal_plan_notifier.dart"
      via: "onAcceptWithDetails calls swapSlots or assignRecipe"
      pattern: "swapSlots|assignRecipe"
    - from: "meal_slot_card.dart"
      to: "planner_grid.dart"
      via: "LongPressDraggable provides MealSlot data to DragTarget"
      pattern: "LongPressDraggable<MealSlot>"
---

<objective>
Add drag-and-drop rescheduling to the planner grid: users can long-press a filled meal card and drag it to any other slot to swap or move meals, with visual feedback and scroll conflict mitigation.

Purpose: Drag-and-drop is the primary rescheduling interaction — without it, users must remove+reassign to move meals between slots, which is tedious.
Output: Working drag-and-drop on the planner grid with persistent swap/move operations.
</objective>

<execution_context>
@C:/Users/tristan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tristan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-weekly-meal-planner/05-CONTEXT.md
@.planning/phases/05-weekly-meal-planner/05-RESEARCH.md
@.planning/phases/05-weekly-meal-planner/05-01-SUMMARY.md
@.planning/phases/05-weekly-meal-planner/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 05-01 and 05-02 — the widgets and notifier we modify -->

MealPlanNotifier methods (from 05-01):
```dart
// mealPlanNotifierProvider(weekStart)
Future<void> swapSlots(String slotIdA, String slotIdB) async;
Future<void> assignRecipe({dayOfWeek, mealType, recipeId, recipeTitle, recipeImage}) async;
Future<void> clearSlot(String slotId) async;
```

MealSlot domain model (from 05-01):
```dart
@freezed sealed class MealSlot {
  const factory MealSlot({
    required String id,
    required String dayOfWeek,
    required String mealType,
    required DateTime weekStart,
    String? recipeId, String? recipeTitle, String? recipeImage,
  }) = _MealSlot;
}
```

Widget structure (from 05-02):
- planner_grid.dart: Row of 7 DayColumns, each Column of 3 slot cells
- meal_slot_card.dart: Filled slot with image, title, replace/remove icons
- empty_slot_card.dart: Dashed border + icon, tap to assign
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LongPressDraggable on filled slots + DragTarget on all slots</name>
  <files>
    meal_mate/lib/features/meal_planner/presentation/widgets/meal_slot_card.dart,
    meal_mate/lib/features/meal_planner/presentation/widgets/planner_grid.dart,
    meal_mate/lib/features/meal_planner/presentation/widgets/empty_slot_card.dart
  </files>
  <action>
1. Update `meal_slot_card.dart` — wrap the card in `LongPressDraggable<MealSlot>`:
   - `data`: the MealSlot object this card represents.
   - `feedback`: a `Material(elevation: 6, borderRadius: BorderRadius.circular(8), child: SizedBox(width: cardWidth, child: _CompactRecipeChip(title, image)))` — a semi-transparent copy of the card at ~80% opacity.
   - `childWhenDragging`: a `Container` with dashed border and reduced opacity (ghosted placeholder showing where the card came from).
   - `hapticFeedbackOnStart: true` for tactile feedback on long-press.
   - `delay: const Duration(milliseconds: 200)` — short delay prevents accidental drags.
   - Keep the existing tap-for-detail and inline icon behaviors unchanged — LongPressDraggable only activates on long-press, so regular taps still work.

2. Update `planner_grid.dart` — wrap each slot cell (both filled and empty) in `DragTarget<MealSlot>`:
   - `onWillAcceptWithDetails`: accept any MealSlot except dropping on the same cell (compare dayOfWeek + mealType).
   - `onAcceptWithDetails`: determine the action based on target state:
     - If target slot has a recipeId (filled): call `notifier.swapSlots(draggedSlot.id, targetSlot.id)` to swap both.
     - If target slot is empty: call `notifier.assignRecipe(...)` with the dragged recipe data for the target slot's day/meal, then call `notifier.clearSlot(draggedSlot.id)` to clear the source. Or better: implement as a "move" that does both in one transaction via swapSlots (since swapSlots with an empty target effectively moves).
   - `builder`: add visual feedback when a drag hovers over this cell. Use `candidateItems.isNotEmpty` to show a highlighted border (e.g., `Border.all(color: Theme.of(context).colorScheme.primary, width: 2)`).
   - The DragTarget wraps the entire cell — both MealSlotCard and EmptySlotCard are children of the DragTarget builder.

3. Update `empty_slot_card.dart` — ensure it works as a DragTarget child:
   - Add an `isHovered` parameter (bool, default false) passed from the DragTarget builder.
   - When `isHovered`, change border color to primary and show a "Drop here" label or highlight effect.
   - The existing tap-to-assign behavior remains unchanged.

4. Scroll conflict mitigation: The `SingleChildScrollView(scrollDirection: Axis.horizontal)` in planner_grid.dart can conflict with drag gestures. Mitigation approach:
   - Add a `ValueNotifier<bool> _isDragging` state to PlannerGrid (make it ConsumerStatefulWidget if needed).
   - When any LongPressDraggable fires `onDragStarted`, set `_isDragging.value = true`. On `onDragEnd` or `onDraggableCanceled`, set to false.
   - Wrap SingleChildScrollView physics: when `_isDragging` is true, use `NeverScrollableScrollPhysics()` to prevent scroll during drag. When false, use default `ClampingScrollPhysics()`.
   - Pass the `_isDragging` notifier down to MealSlotCard via constructor param so it can call `onDragStarted`/`onDragEnd` callbacks.
  </action>
  <verify>
    <automated>cd meal_mate && dart analyze --no-fatal-infos lib/features/meal_planner/presentation/widgets/ 2>&1 | head -20</automated>
  </verify>
  <done>Filled slot cards are draggable via long-press. All slots (filled and empty) accept drops. Dropping on a filled slot swaps meals; dropping on an empty slot moves the meal. Horizontal scroll is disabled during drag to prevent gesture conflicts. Visual feedback shows on hover targets. Changes persist via notifier calls to repository.</done>
</task>

<task type="auto">
  <name>Task 2: Drag feedback polish and edge case handling</name>
  <files>
    meal_mate/lib/features/meal_planner/presentation/widgets/planner_grid.dart,
    meal_mate/lib/features/meal_planner/presentation/widgets/meal_slot_card.dart
  </files>
  <action>
1. Add drag feedback animation: when a drag is in progress, all valid drop targets should show a subtle pulsing border or background tint (use `AnimatedContainer` with duration 200ms). Invalid targets (same cell) remain unchanged.

2. Handle edge cases:
   - Dropping outside any DragTarget (onDraggableCanceled): do nothing, card returns to original position. LongPressDraggable handles this automatically.
   - Rapid successive drags: debounce — after a swap completes, the stream notifier updates state automatically; the UI rebuilds with new positions. No special handling needed since Drift stream fires after write.
   - Empty week (no slots in DB yet): the grid should still show 21 empty cells. If `slotsAsync.value` is empty, generate the full 7x3 grid of EmptySlotCards. Do NOT require pre-seeded rows.

3. Auto-scroll on drag near edges: When dragging a card near the left or right edge of the horizontal scroll view, auto-scroll in that direction. Implementation:
   - In PlannerGrid, use a `Listener` widget that tracks pointer position during drag.
   - If pointer x < 40px from left edge, call `_scrollController.animateTo(current - 60, ...)`.
   - If pointer x > screenWidth - 40px from right edge, scroll right similarly.
   - Only active when `_isDragging` is true.
   - Use a Timer.periodic(100ms) to continuously scroll while near the edge.

4. Ensure the feedback widget (the dragged card) renders above all other content using the default `Overlay` behavior of LongPressDraggable (no extra work needed — Flutter handles this).
  </action>
  <verify>
    <automated>cd meal_mate && dart analyze --no-fatal-infos lib/features/meal_planner/presentation/widgets/ 2>&1 | head -20</automated>
  </verify>
  <done>Drag interactions feel smooth: visual feedback on valid targets, auto-scroll near edges, graceful cancel on drop-outside. Empty weeks render correctly as 21 empty cells. No analyzer errors.</done>
</task>

</tasks>

<verification>
- `dart analyze --no-fatal-infos` passes for all widget files
- LongPressDraggable on MealSlotCard with MealSlot data payload
- DragTarget wraps every slot cell in PlannerGrid
- swapSlots called on drop-onto-filled, move logic on drop-onto-empty
- Horizontal scroll disabled during active drag
- Auto-scroll near horizontal edges during drag
- Empty week renders 21 empty slots without errors
</verification>

<success_criteria>
- Long-pressing a filled slot initiates drag with visual feedback card
- Dragging onto another filled slot swaps the two meals
- Dragging onto an empty slot moves the meal
- Changes persist after navigating away and returning
- Horizontal scroll and drag do not conflict
- Auto-scroll activates when dragging near horizontal edges
</success_criteria>

<output>
After completion, create `.planning/phases/05-weekly-meal-planner/05-03-SUMMARY.md`
</output>
