---
phase: 05-weekly-meal-planner
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - meal_mate/lib/features/meal_planner/presentation/screens/template_list_screen.dart
  - meal_mate/lib/features/meal_planner/presentation/screens/planner_screen.dart
autonomous: true
requirements:
  - PLAN-05
  - PLAN-06

must_haves:
  truths:
    - "User can save the current week as a named template via the planner overflow menu"
    - "User can view a list of saved templates"
    - "User can load a template into the current week with choice of replace-all or fill-empty-only"
    - "User can delete a saved template"
  artifacts:
    - path: "meal_mate/lib/features/meal_planner/presentation/screens/template_list_screen.dart"
      provides: "Template list with load and delete actions"
      min_lines: 60
    - path: "meal_mate/lib/features/meal_planner/presentation/screens/planner_screen.dart"
      provides: "Updated overflow menu with Save/Load template actions"
      contains: "Save as Template"
  key_links:
    - from: "planner_screen.dart"
      to: "template_notifier.dart"
      via: "saveCurrentWeek called from overflow menu dialog"
      pattern: "templateNotifierProvider"
    - from: "template_list_screen.dart"
      to: "template_notifier.dart"
      via: "loadTemplate and deleteTemplate called from list actions"
      pattern: "loadTemplate|deleteTemplate"
---

<objective>
Build the template save/load feature: users can save their current week as a named template from the planner overflow menu, view saved templates on a dedicated screen, and load any template into the current or future week.

Purpose: Templates let users reuse their best meal plans week after week without rebuilding from scratch.
Output: Working save-as-template dialog, template list screen with load/delete, load-mode choice dialog.
</objective>

<execution_context>
@C:/Users/tristan/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/tristan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-weekly-meal-planner/05-CONTEXT.md
@.planning/phases/05-weekly-meal-planner/05-RESEARCH.md
@.planning/phases/05-weekly-meal-planner/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 05-01 — template notifier and repository -->

From meal_mate/lib/features/meal_planner/presentation/providers/template_notifier.dart:
```dart
// templateNotifierProvider
// State: AsyncValue<List<PlanTemplate>>
// Methods:
//   saveCurrentWeek({required String name, required DateTime weekStart})
//   loadTemplate({required String templateId, required DateTime targetWeekStart, required bool replaceAll})
//   deleteTemplate(String templateId)
```

From meal_mate/lib/features/meal_planner/domain/plan_template.dart:
```dart
@freezed sealed class PlanTemplate {
  const factory PlanTemplate({
    required String id,
    required String name,
    required DateTime createdAt,
    required List<MealSlot> slots,
  }) = _PlanTemplate;
}
```

Planner screen overflow menu (from 05-02):
- PopupMenuButton with placeholder "Save as Template" and "Load Template" items
- These need to be wired to actual functionality in this plan
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template list screen with load and delete</name>
  <files>
    meal_mate/lib/features/meal_planner/presentation/screens/template_list_screen.dart
  </files>
  <action>
1. Create `template_list_screen.dart` — a ConsumerWidget (replaces the placeholder from 05-02 if one exists):
   - Watches `ref.watch(templateNotifierProvider)`.
   - AppBar title: "Meal Plan Templates".
   - Body: handle loading (CircularProgressIndicator), error (error text + retry), and data states.
   - Data state: if templates list is empty, show empty state: centered Column with Icon(Icons.bookmark_border, size: 64), Text("No templates saved yet"), Text("Save your current week from the planner to create a template").
   - If templates exist: ListView.builder of template cards.
   - Each template card: Card with ListTile showing `template.name` as title, `DateFormat('MMM d, yyyy').format(template.createdAt)` as subtitle, trailing Row of two IconButtons:
     - Load icon (Icons.download or Icons.file_download): opens a confirmation dialog asking "Load into which week?" with the current week pre-selected and a DatePicker to choose a different target week. After week selection, shows a second dialog: "Replace all meals" or "Fill empty slots only" (two buttons). On confirm, calls `ref.read(templateNotifierProvider.notifier).loadTemplate(templateId: template.id, targetWeekStart: selectedWeek, replaceAll: choice)`. Show SnackBar "Template loaded" on success, then pop back to planner.
     - Delete icon (Icons.delete_outline): shows AlertDialog "Delete template '{name}'? This cannot be undone." with Cancel/Delete buttons. On confirm, calls `ref.read(templateNotifierProvider.notifier).deleteTemplate(template.id)`. Show SnackBar "Template deleted".
   - Each template card also shows a preview: a row of 7 small circles or dots indicating which days have meals (filled dot = at least one meal in that day, empty dot = no meals). This gives users a visual density preview. Compute from `template.slots.where((s) => s.recipeId != null).map((s) => s.dayOfWeek).toSet()`.

2. The screen receives the current `weekStart` as a constructor parameter (so it can pre-fill the "load into current week" option). Pass it from the planner screen when navigating.
  </action>
  <verify>
    <automated>cd meal_mate && dart analyze --no-fatal-infos lib/features/meal_planner/presentation/screens/template_list_screen.dart 2>&1 | head -20</automated>
  </verify>
  <done>TemplateListScreen renders saved templates with name, date, and day-density preview dots. Load action shows week picker then replace/fill dialog. Delete action shows confirmation. Empty state renders when no templates exist.</done>
</task>

<task type="auto">
  <name>Task 2: Wire planner overflow menu to save/load template actions</name>
  <files>
    meal_mate/lib/features/meal_planner/presentation/screens/planner_screen.dart
  </files>
  <action>
1. Update the PopupMenuButton in PlannerScreen's AppBar actions (replacing the placeholder from 05-02):

   - "Save as Template" menu item: on tap, show a `showDialog` with a TextField for template name. Dialog has Cancel and Save buttons. TextField has autofocus, maxLength 30, and hint text "e.g., Busy Week, Veggie Week". On Save: validate name is not empty, call `ref.read(templateNotifierProvider.notifier).saveCurrentWeek(name: name, weekStart: _weekStart)`. Show SnackBar "Template saved as '{name}'" on success. Show error SnackBar if the current week has no filled slots (check first before saving).

   - "Load Template" menu item: on tap, navigate to `context.push('/planner/templates', extra: _weekStart)` passing the current weekStart. Or use query params: `context.push('/planner/templates?week=${_weekStart.millisecondsSinceEpoch}')`. The TemplateListScreen reads this to know which week to pre-fill for loading.

2. Update the route for `/planner/templates` in `meal_planner_routes.dart` (if needed) to pass the weekStart from the route's extra or query parameter to TemplateListScreen's constructor.

3. After a template is loaded (user returns from TemplateListScreen), the MealPlanNotifier stream will automatically update the grid since the Drift writes trigger the reactive stream. No explicit refresh needed.
  </action>
  <verify>
    <automated>cd meal_mate && dart analyze --no-fatal-infos lib/features/meal_planner/presentation/screens/planner_screen.dart lib/features/meal_planner/presentation/meal_planner_routes.dart 2>&1 | head -20</automated>
  </verify>
  <done>Overflow menu "Save as Template" shows name input dialog and saves via notifier. "Load Template" navigates to TemplateListScreen with current weekStart. Route updated to pass weekStart parameter. Full save/load flow works end-to-end.</done>
</task>

</tasks>

<verification>
- `dart analyze --no-fatal-infos` passes for all template-related files
- PlannerScreen overflow menu has "Save as Template" and "Load Template" working entries
- Save dialog validates non-empty name and checks for filled slots
- TemplateListScreen shows empty state when no templates exist
- TemplateListScreen shows template cards with load/delete actions
- Load flow: pick week -> choose replace-all or fill-empty -> template applied
- Delete flow: confirmation dialog -> template removed from list
</verification>

<success_criteria>
- Save as Template: name dialog -> saves current week's filled slots as template
- Load Template: navigates to list -> pick template -> choose week + mode -> slots populated
- Delete Template: confirmation -> removed from list
- Empty state shown when no templates exist
- Planner grid updates reactively after template load
</success_criteria>

<output>
After completion, create `.planning/phases/05-weekly-meal-planner/05-04-SUMMARY.md`
</output>
